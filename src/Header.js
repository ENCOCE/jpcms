export default Header;

function Header(props) {
    return (
        <>
            <div class="header">
                <h1>로고</h1>
                <ul>
                    <li><button onClick={() => { }}>UPDATE DB</button></li>
                </ul>                
            </div>
        </>
    );
}